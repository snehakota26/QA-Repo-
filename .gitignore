# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
/reports/
# Captured Azure Portal sessions are local-development only and expire within hours.
# CI signs in with TOTP instead, so no session artifact is ever committed.
/.auth/

# Secrets
.env

# Playwright MCP scratch snapshots
/.playwright-mcp/
